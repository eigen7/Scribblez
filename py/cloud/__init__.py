"""Cloud-fleet tooling: rented compute (Runpod) generating data into R2.

Library modules for the cloud workflow's laptop-side CLIs (py/scripts/cloud_*)
and the worker entrypoint that runs on rented machines.
"""
