"""Pre-move value model (M_pre) track (docs/roadmap2.md, track A): the
distillation-target data path (.pmt sidecars) plus the training side -- dataset,
model, training loop, and the top-K recall metric against the teacher."""
