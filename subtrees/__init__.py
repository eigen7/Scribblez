"""Package root for the git submodules under subtrees/ (see README.md).

This file exists so submodule packages can be imported as `subtrees.<name>`
from the repo root. It is a project-owned file, not part of any submodule.
"""
