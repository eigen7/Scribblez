"""Namespace package for vendored git subtrees (see README.md).

This file exists so subtree packages can be imported as `subtrees.<name>`
from the repo root. It is a project-owned file, not part of any subtree.
"""
